import s from "./NotFoundPage.module.css";

const NotFoundPage = () => {
    return (
        <div className={s.conteiner}>
            <h1 className={s.title}>This page was not found</h1>
        </div>
    );
};

export default NotFoundPage;